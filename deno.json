{
  "fmt": {
    "semiColons": false,
    "singleQuote": true,
    "exclude": ["dist"]
  },
  "lint": {
    "rules": {
      "exclude": ["jsx-key"]
    }
  },
  "workspace": ["./src/api", "./src/bf", "./src/site"],
  "compilerOptions": {
    "jsx": "react",
    "jsxFactory": "jsx",
    "jsxFragmentFactory": "Fragment"
  },
  "tasks": {
    "create-session": {
      "description": "Create a session and print it to the console",
      "command": "deno run -A --env-file ./scripts/create-session.ts"
    },
    "create-my-session": {
      "description": "Create a session for the current user and print it to the console",
      "command": "deno run -A --env-file ./scripts/create-session.ts --admin"
    },
    "get-tsg-cws": {
      "description": "Get The Story Graph content warning data",
      "command": "deno run -A ./scripts/get-tsg-cws.ts"
    },
    "save-suggestions": {
      "description": "Save suggestions to disk",
      "command": "deno run -A --env-file ./scripts/save-suggestions.ts"
    },
    "save-tally": {
      "description": "Save a tally to disk",
      "command": "deno run -A --env-file ./scripts/save-tally.ts"
    }
  }
}
